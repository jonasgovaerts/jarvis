---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: managedrepositories.jarvis.dev
spec:
  group: jarvis.dev
  names:
    kind: ManagedRepository
    listKind: ManagedRepositoryList
    plural: managedrepositories
    singular: managedrepository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.owner
      name: Owner
      type: string
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .status.activeWorkItems
      name: Active
      type: integer
    - jsonPath: .spec.suspend
      name: Suspended
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedRepository configures one repository Jarvis watches and
          works on.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedRepositorySpec defines the desired state of ManagedRepository
            properties:
              credentialsSecretRef:
                description: |-
                  Secret (same namespace, key "token") used to read issues, clone, push
                  branches, open PRs and read checks.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              gitops:
                description: |-
                  GitOpsSpec maps a repository to its GitOps deployment location. Repos
                  without it skip the RolloutCheck phase.
                properties:
                  argocdApp:
                    type: string
                  credentialsSecretRef:
                    description: Secret (same namespace, key "token") with push rights
                      on the gitops repo.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  manifestStyle:
                    default: KustomizeImage
                    description: Hint for the SRE agent on how image tags appear in
                      the manifests.
                    enum:
                    - KustomizeImage
                    - HelmValues
                    type: string
                  path:
                    description: Directory inside the gitops repo, e.g. "apps/myservice".
                    type: string
                  repoUrl:
                    type: string
                  targetBranch:
                    default: main
                    type: string
                  updateStrategy:
                    default: PullRequest
                    enum:
                    - DirectPush
                    - PullRequest
                    type: string
                required:
                - credentialsSecretRef
                - path
                - repoUrl
                type: object
              issueSelector:
                description: |-
                  IssueSelector controls which provider issues become WorkItems. An empty
                  RequireLabels means every open issue is picked up — the recommended setup
                  is an explicit opt-in label like "jarvis".
                properties:
                  excludeLabels:
                    items:
                      type: string
                    type: array
                  requireLabels:
                    items:
                      type: string
                    type: array
                type: object
              name:
                type: string
              owner:
                type: string
              pipeline:
                description: PipelineSpec configures pipeline behavior per repository.
                properties:
                  agents:
                    additionalProperties:
                      description: AgentSpec tunes one pipeline stage for this repository.
                      properties:
                        image:
                          description: Override the default agent image.
                          type: string
                        maxRetries:
                          format: int32
                          type: integer
                        model:
                          description: ModelSpec selects an LLM via the LiteLLM proxy's
                            logical model names.
                          properties:
                            maxTokens:
                              format: int32
                              type: integer
                            model:
                              description: LiteLLM logical model name, e.g. "claude-sonnet".
                              type: string
                            temperature:
                              description: Stringified float to keep CRDs integer/string-only,
                                e.g. "0.2".
                              type: string
                          required:
                          - model
                          type: object
                        timeoutSeconds:
                          description: Job activeDeadlineSeconds for this stage.
                          format: int64
                          type: integer
                      type: object
                    description: 'Keys: analyzer, developer, devops, sre.'
                    type: object
                  autoMerge:
                    description: |-
                      AutoMerge lets the devops agent squash-merge green PRs; default is a
                      human merge after the pr.ready notification.
                    type: boolean
                  maxConcurrentWorkItems:
                    default: 2
                    format: int32
                    type: integer
                type: object
              provider:
                enum:
                - github
                - gitlab
                type: string
              suspend:
                description: Suspend stops new WorkItem creation and pauses phase
                  transitions.
                type: boolean
            required:
            - credentialsSecretRef
            - name
            - owner
            - provider
            type: object
          status:
            description: ManagedRepositoryStatus defines the observed state of ManagedRepository.
            properties:
              activeWorkItems:
                description: Count of non-terminal WorkItems referencing this repository.
                format: int32
                type: integer
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastIssueSync:
                description: Patched by the issue-watcher after each successful poll.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
